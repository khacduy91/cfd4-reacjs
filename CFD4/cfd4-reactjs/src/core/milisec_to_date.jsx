const milisec_to_date = (date, format) => {
  switch (format) {
    case "yyyy-mm-dd":
      let arrayDate = date.split("-");

      break;

    default:
      break;
  }
};

export default milisec_to_date;
